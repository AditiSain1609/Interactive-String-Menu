# Interactive-String-Menu
A menu-driven Python project that performs various string operations such as strip spaces, find substring, capitalize, uppercase, lowercase, title case, replace words, count occurrences, startswith, and endswith.
